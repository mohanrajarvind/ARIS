import SwiftUI

struct SettingsView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("Settings")
                .foregroundStyle(.white)
        }
    }
}
